

var React = require('react');
//You need this npm package to do createReactClass
var CurrentProjects = require('create-react-class');

    module.exports=CurrentProjects({
            render:function(){
               return(
                <div>
                
              </div>
                );
            }
        })
